replicaCount: 1

image:
  tag: "latest"

resources:
  limits:
    cpu: 250m
    memory: 256Mi
  requests:
    cpu: 50m
    memory: 128Mi

ingress:
  enabled: true
  hosts:
    - host: wedding-hall.dev.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
